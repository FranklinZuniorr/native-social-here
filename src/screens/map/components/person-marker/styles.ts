import { StyleSheet } from 'react-native';

export const stylesPersonMarker = StyleSheet.create({
    container: {
        justifyContent: 'center',
        alignItems: 'center',
        gap: 4,
    },
    image: {
        width: 50,
        height: 50,
    },
    title: {
        fontFamily: 'Archivo Black',
        textAlign: 'center',
        fontSize: 14,
    },
});
